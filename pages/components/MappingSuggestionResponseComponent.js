

import React from 'react';
import { View } from 'react-native';
import { Container, Header, Tab, Tabs, TabHeading, Grid, Text } from 'native-base';
import SingleTabComponent from './SingleTabComponent';

const MappingSuggestionResponseComponent = (props) => {
    return (
        <Container style={{height:120, borderWidth:1}}>
            <Text>FirstName</Text>
            <Text>******</Text>
            <Text>Can help with</Text>

        </Container>
    )

}

export default MappingSuggestionResponseComponent;